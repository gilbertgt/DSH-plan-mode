# Overlay for worktree-isolated SDK Workers. The sdk profile already composes
# dsh-base; this file tightens filesystem policy and makes the Worker a leaf.
- id: sandbox-policy
  config:
    mode: workspace-write
- id: tool-subagent
  disabled: true
- id: tool-subagent-fork
  disabled: true
