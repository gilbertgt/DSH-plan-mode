import { mkdir, rm } from 'node:fs/promises';import { join, resolve, sep } from 'node:path';import { git } from './repository.ts';import { confined, stateRoot } from '../recovery/store.ts'
export interface WorktreeLease{path:string;runId:string;taskId:string;baseHead:string}
export async function createWorktree(repo:string,runId:string,taskId:string,head:string,root=stateRoot()):Promise<WorktreeLease>{const path=confined(root,'worktrees',runId,taskId);await mkdir(join(path,'..'),{recursive:true});await git(repo,['worktree','add','--detach',path,head]);return{path,runId,taskId,baseHead:head}}
export async function removeOwnedWorktree(repo:string,lease:WorktreeLease,force=false){await git(repo,['worktree','remove',...(force?['--force']:[]),lease.path]);await rm(lease.path,{recursive:true,force:true}).catch(()=>{})}

export async function cleanupRunWorktrees(repo:string,runId:string,root=stateRoot()){const runRoot=resolve(confined(root,'worktrees',runId));const listed=(await git(repo,['worktree','list','--porcelain','-z'])).stdout.toString('utf8').split('\0');for(const rec of listed){const line=rec.split('\n').find(x=>x.startsWith('worktree '));if(!line)continue;const path=resolve(line.slice(9));if(path!==runRoot&&!path.startsWith(runRoot+sep))continue;await git(repo,['worktree','remove','--force',path]).catch(()=>{});await rm(path,{recursive:true,force:true}).catch(()=>{})}}
