export class RunCancellation{readonly controller=new AbortController();#closers=new Set<()=>Promise<void>|void>();get signal(){return this.controller.signal}track(close:()=>Promise<void>|void){this.#closers.add(close);return()=>this.#closers.delete(close)}async cancel(reason='user requested stop'){if(!this.controller.signal.aborted)this.controller.abort(reason);await Promise.allSettled([...this.#closers].map(fn=>Promise.resolve().then(fn)));this.#closers.clear()}}
