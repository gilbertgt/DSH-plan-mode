import { losslessRouteChoice, type RoleName, type RoleRoute, type RouteChoice } from '../contract/settings.ts'
export function routeChoices(route:RoleRoute,current:RouteChoice):RouteChoice[]{const primary=route.mode==='current'?current:{provider:route.provider!,model:route.model!,...(route.reasoningEffort!==undefined?{reasoningEffort:route.reasoningEffort}:{}),...(route.maxTokens!==undefined?{maxTokens:route.maxTokens}:{})};return [primary,...route.fallbacks].map(losslessRouteChoice).filter((v,i,a)=>v.provider&&v.model&&a.findIndex(x=>x.provider===v.provider&&x.model===v.model&&x.reasoningEffort===v.reasoningEffort)===i)}
export async function preflightRoute(llm:any, choice:RouteChoice){if(typeof llm?.resolveCallConfig!=='function')throw new Error('llm.resolveCallConfig unavailable');await llm.resolveCallConfig(choice);return choice}
export function roleLabel(role:RoleName,taskId?:string){return taskId?`${role}:${taskId}`:role}
