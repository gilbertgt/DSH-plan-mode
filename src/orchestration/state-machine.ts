import type { RunPhase } from '../contract/events.ts'
const allowed:Record<RunPhase,Set<RunPhase>>={
IDLE:new Set(['PLANNING']),PLANNING:new Set(['PLAN_VALIDATED','CANCELLED','FAILED']),PLAN_VALIDATED:new Set(['APPROVED_PENDING','PLANNING','CANCELLED']),APPROVED_PENDING:new Set(['PREFLIGHT','CANCELLED','FAILED']),PREFLIGHT:new Set(['WORKERS','BLOCKED','FAILED','CANCELLED']),WORKERS:new Set(['INTEGRATING','VALIDATING','BLOCKED','FAILED','CANCELLED']),INTEGRATING:new Set(['VALIDATING','BLOCKED','FAILED','CANCELLED']),VALIDATING:new Set(['REVIEWING','BLOCKED','FAILED','CANCELLED']),REVIEWING:new Set(['COMPLETE','FIXING','FAILED','BLOCKED','CANCELLED']),FIXING:new Set(['VALIDATING','FAILED','BLOCKED','CANCELLED']),COMPLETE:new Set(),BLOCKED:new Set(),FAILED:new Set(),INTERRUPTED:new Set(['PREFLIGHT','WORKERS','VALIDATING','REVIEWING','BLOCKED','CANCELLED']),CANCELLED:new Set()}
export const TERMINAL=new Set<RunPhase>(['COMPLETE','BLOCKED','FAILED','CANCELLED'])
export class RunStateMachine {phase:RunPhase;constructor(phase:RunPhase='IDLE'){this.phase=phase} transition(next:RunPhase){if(next==='INTERRUPTED'&&!TERMINAL.has(this.phase)){this.phase=next;return}if(!allowed[this.phase].has(next))throw new Error(`invalid run transition ${this.phase} -> ${next}`);this.phase=next}}
