import test from 'node:test';import assert from 'node:assert/strict';import {assertOwnedPaths,disjointOwnership} from '../../src/git/ownership.ts';test('ownership is fail closed',()=>{assert.ok(assertOwnedPaths(['a.ts'],['a.ts'],'linux'));assert.throws(()=>assertOwnedPaths(['b.ts'],['a.ts'],'linux'),/violation/);assert.equal(disjointOwnership(['A.ts'],['a.ts'],'win32'),false);assert.equal(disjointOwnership(['A.ts'],['a.ts'],'linux'),true)})
