import test from 'node:test';import assert from 'node:assert/strict';import {parseReviewVerdict} from '../../src/orchestration/reviewer.ts';test('strict marker protocol',()=>{assert.equal(parseReviewVerdict('[REVIEW:PASS]\nok').kind,'PASS');assert.equal(parseReviewVerdict('prefix\n[REVIEW:PASS]').kind,'PROTOCOL_INVALID');assert.equal(parseReviewVerdict('[REVIEW:FAIL]\n[REVIEW:PASS]').kind,'PROTOCOL_INVALID')})
